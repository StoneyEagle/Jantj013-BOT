
module.exports = {
	name: 'rateLimit',
	/**
	 * @param {import('discord.js').Client} client
	 * @param {import('discord.js').RateLimitData} rateLimitInfo
	 * @param {string[]} args
	 */
	run: async (client, rateLimitInfo, args) => {
		// console.log(rateLimitInfo, args);
		
	},
};