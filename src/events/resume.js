
module.exports = {
	name: 'resume',
	/**
	 * @param {import('discord.js').Client} client
	 * @param {import('discord.js').WSEventType} replayed
	 * @param {string[]} args
	 */
	run: async (client, replayed, args) => {
		// console.log(replayed, args);
		
	},
};