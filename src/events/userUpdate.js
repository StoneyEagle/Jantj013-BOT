
module.exports = {
	name: 'userUpdate',
	/**
	 * @param {import('discord.js').Client} client
	 * @param {import('discord.js').GuildMember} oldUser
	 * @param {import('discord.js').GuildMember} newUser
	 * @param {string[]} args
	 */
	run: async (client, oldUser, newUser, args) => {
		// console.log(oldUser, newUser, args);
		
	},
};