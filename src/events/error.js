
module.exports = {
	name: 'error',
	/**
	 * @param {import('discord.js').Client} client
	 * @param {import('discord.js').WSEventType} error
	 * @param {string[]} args
	 */
	run: async (client, error, args) => {
		// console.log(error, args);
		
	},
};