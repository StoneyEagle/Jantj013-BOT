
module.exports = {
	name: 'clientUserSettingsUpdate',
	/**
	 * @param {import('discord.js').Client} client
	 * @param {import('discord.js').ClientUser} clientUserSettings
	 * @param {string[]} args
	 */
	run: async (client, clientUserSettings, args) => {
		// console.log(channel, args);
		
	},
};