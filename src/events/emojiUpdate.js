
module.exports = {
	name: 'emojiUpdate',
	/**
	 * @param {import('discord.js').Client} client
	 * @param {import('discord.js').Emoji} oldEmoji
	 * @param {import('discord.js').Emoji} newEmoji
	 * @param {string[]} args
	 */
	run: async (client, oldEmoji, newEmoji, args) => {
		// console.log(oldEmoji, newEmoji, args);
		
	},
};