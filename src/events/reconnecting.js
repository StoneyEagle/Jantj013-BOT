
module.exports = {
	name: 'reconnecting',
	/**
	 * @param {import('discord.js').Client} client
	 * @param {string[]} args
	 */
	run: async (client, args) => {
		// console.log(client, args);
		
	},
};