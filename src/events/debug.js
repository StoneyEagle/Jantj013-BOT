
module.exports = {
	name: 'debug',
	/**
	 * @param {import('discord.js').Client} client
	 * @param {string} info
	 * @param {string[]} args
	 */
	run: async (client, info, args) => {
		// console.log(info, args);
		
	},
};