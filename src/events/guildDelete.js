
module.exports = {
	name: 'guildDelete',
	/**
	 * @param {import('discord.js').Client} client
	 * @param {import('discord.js').Guild} guild
	 * @param {string[]} args
	 */
	run: async (client, guild, args) => {
		// console.log(guild, args);
		
	},
};
