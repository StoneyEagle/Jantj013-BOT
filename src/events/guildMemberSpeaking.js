
module.exports = {
	name: 'guildMemberSpeaking',
	/**
	 * @param {import('discord.js').Client} client
	 * @param {import('discord.js').GuildMember} member
	 * @param {string[]} args
	 */
	run: async (client, member, speaking, args) => {
		// console.log(member, speaking, args);
		
	},
};