
module.exports = {
	name: 'userNoteUpdate',
	/**
	 * @param {import('discord.js').Client} client
	 * @param {import('discord.js').GuildMember} user
	 * @param {import('discord.js').Message} oldNote
	 * @param {import('discord.js').Message} newNote
	 * @param {string[]} args
	 */
	run: async (client, user, oldNote, newNote, args) => {
		// console.log(user, oldNote, newNote, args);
		
	},
};