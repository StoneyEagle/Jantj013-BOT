
module.exports = {
	name: 'guildUpdate',
	/**
	 * @param {import('discord.js').Client} client
	 * @param {import('discord.js').Guild} oldGuild
	 * @param {import('discord.js').Guild} newGuild
	 * @param {string[]} args
	 */
	run: async (client, oldGuild, newGuild, args) => {
		// console.log(oldGuild, newGuild, args);
		
	},
};