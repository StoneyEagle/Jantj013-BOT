
module.exports = {
	name: 'messageUpdate',
	/**
	 * @param {import('discord.js').Client} client
	 * @param {import('discord.js').Message} oldMessage
	 * @param {import('discord.js').Message} newMessage
	 * @param {string[]} args
	 */
	run: async (client, oldMessage, newMessage, args) => {
		// console.log(oldMessage, newMessage, args);
		
	},
};