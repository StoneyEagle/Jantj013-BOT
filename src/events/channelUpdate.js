
module.exports = {
	name: 'channelUpdate',
	/**
	 * @param {import('discord.js').Client} client
	 * @param {import('discord.js').Channel} oldChannel
	 * @param {import('discord.js').Channel} newChannel
	 * @param {string[]} args
	 */
	run: async (client, oldChannel, newChannel, args) => {
		// console.log(channel, args);
		
	},
};