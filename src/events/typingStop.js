
module.exports = {
	name: 'typingStop',
	/**
	 * @param {import('discord.js').Client} client
	 * @param {import('discord.js').Channel} channel
	 * @param {import('discord.js').GuildMember} user
	 * @param {string[]} args
	 */
	run: async (client, channel, user, args) => {
		// console.log(channel,user, args);
		
	},
};