
module.exports = {
	name: 'clientUserGuildSettingsUpdate',
	/**
	 * @param {import('discord.js').Client} client
	 * @param {import('discord.js').ClientUser} clientUserGuildSettings
	 * @param {string[]} args
	 */
	run: async (client,clientUserGuildSettings, args) => {
		// console.log(channel, args);
		
	},
};