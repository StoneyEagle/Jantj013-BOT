
module.exports = {
	name: 'channelPinsUpdate',
	/**
	 * @param {import('discord.js').Client} client
	 * @param {import('discord.js').Channel} channel
	 * @param {Date} time
	 * @param {string[]} args
	 */
	run: async (client, channel, time, args) => {
		// console.log(channel,time, args);
		
	},
};