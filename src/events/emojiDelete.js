
module.exports = {
	name: 'emojiDelete',
	/**
	 * @param {import('discord.js').Client} client
	 * @param {import('discord.js').Emoji} emoji
	 * @param {string[]} args
	 */
	run: async (client, emoji, args) => {
		// console.log(emoji, args);
		
	},
};